int n=scan.nextInt();
	    if(n==0)
	        System.out.println(0);
	    else{
	        int num= (int)((Math.log(n & ~(n-1))/Math.log(2))+1);   //first set bit of a number
	        System.out.println(num);
	        }
